const Header = ({style}) => {
    return (
        <header className={'flex flex-center'} style={style}>
            <h1>OWO what's this?</h1>
            <p>Enter a word to search for gifs</p>
        </header>
    )
}

export default Header
